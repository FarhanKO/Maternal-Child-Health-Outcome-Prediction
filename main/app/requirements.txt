# Dashboard only. Install alongside the root requirements.txt, which carries
# the model dependencies the bundles need in order to unpickle.
streamlit>=1.32
pandas>=2.1
numpy>=1.26
joblib>=1.3
scikit-learn>=1.6
