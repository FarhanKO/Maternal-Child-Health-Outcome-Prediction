# Core
numpy>=1.26
pandas>=2.1
pyarrow
scipy>=1.11
scikit-learn>=1.6          # FrozenEstimator, used by the ensemble calibrator
joblib>=1.3

# Imbalance handling
imbalanced-learn>=0.12

# Gradient boosting
xgboost>=2.0
lightgbm>=4.1
catboost>=1.2

# Current-generation tabular learners
# RealMLP and TabM, published as tuned defaults and never searched over.
pytabkit>=1.5
torch>=2.2

# Explainability, uncertainty, anomaly detection
shap>=0.44
mapie>=0.8
pyod>=1.1

# Plotting
matplotlib>=3.8
seaborn>=0.13

# Optional. TabPFN is a gated download whose first call opens a licence prompt
# in a browser, so it cannot be installed non-interactively in CI. The panel
# skips it cleanly when absent.
# tabpfn>=2.0
